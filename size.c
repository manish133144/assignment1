#include <stdio.h>

int main() {
   int a;
   float b;
   char c;
   double d;
   printf("size of interger: %d",sizeof(a));
   printf("\nsize of float: %d",sizeof(b));
   printf("\nsize of char: %d",sizeof(c));
   printf("\nsize of double: %d",sizeof(d));

    return 0;
}